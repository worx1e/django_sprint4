from django.test import TestCase


class PagesSmokeTest(TestCase):
    def test_pages_exist(self):
        # Небольшой "тест-заглушка" — гарантирует, что импорт используется
        self.assertTrue(True)
